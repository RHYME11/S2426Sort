 i am a new edit

## Histogram output

`Histogramer::Close()` writes histograms to
`histOutput/hist<run>_<subrun:03>.root`. Histogram directory names may contain
`/`; each path component is written as a nested ROOT directory.
